/** v294 · Preparation Queue route. v292c route convention; navigation
 *  registration remains the standing outstanding item (shell registry never
 *  supplied). */
import PreparationQueue from "@/components/preparation/PreparationQueue";
export default function PreparationQueuePage() { return <PreparationQueue />; }
